def add_it(x, z, f, y=1, a=2):
    """
    Возвращает x + z + f + y + a.
    :параметры: целые числа.
    :return: целочисленная сумма парметров.
    """
    return x + z + f + y + a
result = add_it(2, 3, 4)
print(result)
